API reference
=============

Autogenerated reference for every module under ``src/``.

.. note::
   ``dashboard.app`` is excluded on purpose: it is the Streamlit subprocess
   entry point launched by :py:meth:`dashboard.Dashboard.run` (it calls
   Streamlit commands at import time and is never meant to be imported
   directly). Use :py:class:`dashboard.Dashboard` instead.

.. autosummary::
   :toctree: generated
   :recursive:

   simulation
   models
   analysis
   numerical_solver
   symbolic_solver
   backends
   logger
   main

.. autosummary::
   :toctree: generated

   dashboard.Dashboard
